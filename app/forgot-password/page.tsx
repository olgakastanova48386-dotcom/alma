"use client";
import Link from "next/link"; import { FormEvent,useState } from "react";
export default function ForgotPasswordPage(){const[email,setEmail]=useState("");const[code,setCode]=useState("");const[password,setPassword]=useState("");const[repeat,setRepeat]=useState("");const[step,setStep]=useState<1|2|3>(1);const[loading,setLoading]=useState(false);const[error,setError]=useState("");
async function sendCode(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");setLoading(true);try{const r=await fetch("/api/auth/send-code",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,purpose:"reset"})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Не удалось отправить код.");setStep(2)}catch(x){setError(x instanceof Error?x.message:"Не удалось отправить код.")}finally{setLoading(false)}}
async function reset(e:FormEvent<HTMLFormElement>){e.preventDefault();setError("");if(!/^\d{6}$/.test(code)){setError("Введите шестизначный код из письма.");return}if(password.length<8){setError("Новый пароль должен содержать минимум 8 символов.");return}if(password!==repeat){setError("Пароли не совпадают.");return}setLoading(true);try{const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,code,password})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Не удалось восстановить доступ.");setStep(3)}catch(x){setError(x instanceof Error?x.message:"Не удалось восстановить доступ.")}finally{setLoading(false)}}
const ic="w-full rounded-2xl border border-black/20 px-4 py-3.5 outline-none focus:border-black";return <main className="min-h-screen bg-[#f7f4ef] flex items-center justify-center px-4 pt-24 pb-12"><div className="w-full max-w-[430px] rounded-[30px] bg-white p-8 shadow-xl border border-black/5"><p className="text-center text-xs uppercase tracking-[.22em] text-neutral-400">ALMA</p><h1 className="mt-3 text-center text-3xl font-bold">Восстановление доступа</h1>{step===1&&<form onSubmit={sendCode} className="mt-7"><p className="mb-4 text-center text-sm text-neutral-500">Введите почту, привязанную к аккаунту. Мы отправим только код восстановления — никакого спама.</p><input value={email} onChange={e=>setEmail(e.target.value)} type="email" inputMode="email" autoComplete="email" placeholder="Электронная почта" className={ic}/>{error&&<div className="mt-4 rounded-2xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<button disabled={loading||!email} className="mt-5 w-full rounded-2xl bg-black py-3.5 font-semibold text-white disabled:opacity-40">{loading?"Отправляем…":"Получить код"}</button></form>}{step===2&&<form onSubmit={reset} className="mt-7 space-y-3"><p className="text-center text-sm text-neutral-500">Введите код из письма и придумайте новый пароль.</p><input value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,"").slice(0,6))} inputMode="numeric" autoComplete="one-time-code" placeholder="Код из письма" className={ic}/><input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Новый пароль · минимум 8 символов" className={ic}/><input value={repeat} onChange={e=>setRepeat(e.target.value)} type="password" placeholder="Повторите новый пароль" className={ic}/>{error&&<div className="rounded-2xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}<button disabled={loading} className="w-full rounded-2xl bg-black py-3.5 font-semibold text-white disabled:opacity-40">{loading?"Сохраняем…":"Сохранить новый пароль"}</button></form>}{step===3&&<div className="mt-7 text-center"><div className="rounded-[22px] bg-green-50 p-5 text-sm text-green-800">Пароль изменён. Теперь можно войти в ALMA.</div><Link href="/login" className="mt-5 inline-flex rounded-full bg-black px-6 py-3 text-sm font-semibold text-white">Перейти ко входу</Link></div>}<Link href="/login" className="mt-6 block text-center text-sm text-neutral-500 underline">← Вернуться ко входу</Link></div></main>}
